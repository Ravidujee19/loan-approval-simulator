apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: evaluator-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: { name: evaluator-api, port: { number: 80 } }
